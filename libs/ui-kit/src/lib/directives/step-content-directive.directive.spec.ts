/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StepContentDirectiveDirective } from './step-content-directive.directive';

describe('Directive: StepContentDirective', () => {
  it('should create an instance', () => {
    const directive = new StepContentDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
